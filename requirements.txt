# Stock Price Prediction — dependencies
# Pin versions in your own environment for reproducibility (see PROJECT_PLAN.md §1).
# NOTE: install TA-Lib's C library first (conda install -c conda-forge ta-lib),
# then pip install TA-Lib.

torch>=2.0
yfinance
TA-Lib
pandas
numpy
scikit-learn
wandb
matplotlib
seaborn
tqdm
PyYAML
